steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_IMAGE_TAG', '-f', 'Dockerfile.trainer', '.']
images:
  - '$_IMAGE_TAG'
substitutions:
  _IMAGE_TAG: 'us-central1-docker.pkg.dev/${PROJECT_ID}/hyperion-quant/model-trainer:latest'
